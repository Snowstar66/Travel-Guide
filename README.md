# Travel Guide

## Run the Next.js app

From the project root:

```powershell
pnpm dev
```

Then open:

`http://localhost:3000`

## Production check

```powershell
pnpm build
```

## Main files

- `app/page.tsx`
- `components/trip-companion-app.tsx`
- `lib/trip-data.ts`
- `app/globals.css`

## Legacy static prototype

The earlier static prototype is still available in:

- `prototype/index.html`
